services:
  - type: web
    name: jane-snap-vip
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_VERSION
        value: 20.19.0
      - key: DATA_DIR
        value: /var/data
